spring:
  application:
    name: nyeuni
  datasource:
    url: jdbc:h2:mem:unidb                # itt éri el adatbázist
    username: sa
    password: password
    driverClassName: org.h2.Driver     # megmutatja, milyen driver kell hozzá, nem mindegy h sprong 
      vagy sql.
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
h2:
  console:
    enabled: true                               # bekapcsolás
    path: /h2-console                          # útvonal
